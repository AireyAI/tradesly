{
  "name": "tradesly",
  "version": "0.1.0",
  "description": "Tradesly — UK quote-matching marketplace connecting homeowners with vetted local trades.",
  "type": "module",
  "scripts": {
    "serve": "node serve.mjs",
    "screenshot": "node screenshot.mjs",
    "build": "tailwindcss -i src/input.css -o dist/styles.css --minify",
    "watch": "tailwindcss -i src/input.css -o dist/styles.css --watch"
  },
  "dependencies": {
    "puppeteer": "^22.15.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
